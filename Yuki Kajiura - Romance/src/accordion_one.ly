accordionOneSolo = \relative a' {
  \defaultTempo
  \defaultKey
  \defaultTime
  R1*4
  \bar "|."
}

accordionOneBass = \relative a' {
  \defaultTempo
  \defaultKey
  \defaultTime
  R1*4
  \bar "|."
}
